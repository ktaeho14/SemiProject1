SELECT * FROM ANIMAL;


CREATE TABLE ANIMAL_WITNESS(
	NO NUMBER PRIMARY KEY,
	COLOR VARCHAR2(300) NOT NULL,
	KIND VARCHAR2(300) NOT NULL,
	PLACE VARCHAR2(300) NOT NULL,
	SPECIALMARK VARCHAR2(500) NOT NULL,
	WITNESS_PIC VARCHAR2(1000) NOT NULL,
	CONTENT VARCHAR2 (2000) NOT NULL,
	WITNESS_DATE DATE NOT NULL,
	PHONE_NO VARCHAR2(300) NOT NULL,
	CITY VARCHAR2(300) NOT NULL,
	WRITER VARCHAR2(200) NOT NULL
	);

	
CREATE SEQUENCE WITNESS_SQ NOCACHE;

DROP SEQUENCE WITNESS_SQ;
DROP TABLE ANIMAL_WITNESS;

SELECT * FROM ANIMAL_WITNESS;





--SELECT * FROM(SELECT ROWNUM RN, ANIMAL_NO, AGE, CAREADDR, CARENM, CARETEL, CHARGENM, COLORCD, DESERTIONNO, FILENAME, HAPPENDT, HAPPENPLACE, KINDCD, NEUTERYN, NOTICEEDT, NOTICENO, NOTICESDT, OFFICETEL, ORGNM, POPFILE, PROCESSSTATE, SEXCD, SPECIALMARK, WEIGHT FROM
--(SELECT * FROM ANIMAL ORDER BY ANIMAL_NO DESC)) WHERE KINDCD LIKE '개'||'%' AND RN BETWEEN 1 AND 10;


	